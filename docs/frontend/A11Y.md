# Frontend accessibility baseline

- `lang="fa"` + `dir="rtl"` on screens
- Skip link to `#root` in index.html
- `:focus-visible` outline on interactive controls
- FormField: label + optional hint + `role="alert"` field errors
- LoadingBlock uses `aria-live="polite"`
- ErrorBlock uses `role="alert"`
