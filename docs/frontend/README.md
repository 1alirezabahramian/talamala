# Talamala Frontend
- `frontend/customer` — OTP, shell, assets, orders, custody
- `frontend/backoffice` — staff, registration queue, custody ops
Rules: RTL, no client money math, existing OpenAPI only.
Shared UI: each app `src/ui/` (LoadingBlock, ErrorBlock, EmptyBlock, StatusBadge).
