# Talago continuation (2026-08-13)

## Done this session
- Minimal HTTP front controller: `backend/public/index.php`
- Autoload without Composer: `backend/app/bootstrap_autoload.php`
- CLI smoke (8/8 pass): `php backend/bin/smoke.php`
- ADR-002 Kimia truth · ADR-003 Custody · ADR-004 Immutable quotes

## GitHub write from connector
Still 403 — owner continues with local `git push`.

## Smoke vertical
tenant → register → bind → assets → custody → order accept → settlement blocked
